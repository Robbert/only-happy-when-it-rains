/*
 * Form data submitted by the token form.
 *
 * Can either be used server-side handling the POST request, or client side using `ngSubmit`.
 */
export interface TokenFormData {
    token: string;
}
