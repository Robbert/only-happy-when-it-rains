describe('Form to provide API key', () => {
    it.skip('should allow the user to enter the token', () => {});

    it.skip('should not visibly display the token to prevent leaking the token', () => {});

    it.skip('should emit the token when the user submits the form', () => {});

    it.skip('should only submit the token when the optional validation step has been successful', () => {});
});
