// tslint:disable:ordered-imports

// Zone JS is required by default for Angular itself,
// Zone JS MUST be included before all others, therefore ignore linting for import order.
import 'zone.js/dist/zone';
